{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Spot Navigation",
  "description": "Configuration for the production Spot navigation custom module.",
  "type": "object",
  "properties": {
    "mapImageStreamName": {
      "title": "Global Map Image Stream",
      "description": "Rendered GraphNav global map image used as the display layer.",
      "type": "string",
      "default": "spot.localization.graphnav.global.image",
      "$formant.streams.byType": "image"
    },
    "mapImageMetadataStreamName": {
      "title": "Global Map Image Metadata Stream",
      "description": "JSON metadata companion for the rendered global map image.",
      "type": "string",
      "default": "spot.localization.graphnav.global.image.meta",
      "$formant.streams.byType": "json"
    },
    "overlayStreamName": {
      "title": "Waypoint Overlay Stream",
      "description": "Small JSON overlay stream with waypoint data for the current map.",
      "type": "string",
      "default": "spot.graphnav.overlay",
      "$formant.streams.byType": "json"
    },
    "navStateStreamName": {
      "title": "Navigation State Stream",
      "description": "Current GraphNav status and live seed-frame pose.",
      "type": "string",
      "default": "spot.nav.state",
      "$formant.streams.byType": "json"
    },
    "mapsStreamName": {
      "title": "Saved Maps Stream",
      "description": "Realtime text stream with newline-separated saved map IDs.",
      "type": "string",
      "default": "spot.maps",
      "$formant.streams.byType": "text"
    },
    "currentMapStreamName": {
      "title": "Current Map Stream",
      "description": "Realtime text stream with the active GraphNav map ID.",
      "type": "string",
      "default": "spot.map.current",
      "$formant.streams.byType": "text"
    },
    "defaultMapStreamName": {
      "title": "Default Map Stream",
      "description": "Realtime text stream with the adapter default map ID.",
      "type": "string",
      "default": "spot.map.default",
      "$formant.streams.byType": "text"
    },
    "connectionStateStreamName": {
      "title": "Connection State Stream",
      "description": "One-word Spot connection state.",
      "type": "string",
      "default": "spot.connection_state",
      "$formant.streams.byType": "text"
    },
    "dockingStateStreamName": {
      "title": "Docking State Stream",
      "description": "One-word Spot docking state.",
      "type": "string",
      "default": "spot.docking_state",
      "$formant.streams.byType": "text"
    },
    "motorPowerStateStreamName": {
      "title": "Motor Power State Stream",
      "description": "One-word motor power state.",
      "type": "string",
      "default": "spot.motor_power_state",
      "$formant.streams.byType": "text"
    },
    "behaviorStateStreamName": {
      "title": "Behavior State Stream",
      "description": "One-word robot behavior state.",
      "type": "string",
      "default": "spot.behavior_state",
      "$formant.streams.byType": "text"
    },
    "batteryStreamName": {
      "title": "Battery Stream",
      "description": "Numeric Spot battery percentage stream.",
      "type": "string",
      "default": "spot.robot_state.battery",
      "$formant.streams.byType": "numeric"
    },
    "mapLoadCommandName": {
      "title": "Load Map Command Name",
      "description": "Command template used to load a saved GraphNav map.",
      "type": "string",
      "default": "spot.map.load"
    },
    "mapSetDefaultCommandName": {
      "title": "Set Default Map Command Name",
      "description": "Command template used to set the adapter default map.",
      "type": "string",
      "default": "spot.map.set_default"
    },
    "waypointSaveCommandName": {
      "title": "Waypoint Save Command Name",
      "description": "Command template used to save the robot's current pose as a waypoint on the active map.",
      "type": "string",
      "default": "spot.waypoint.save"
    },
    "waypointDeleteCommandName": {
      "title": "Waypoint Delete Command Name",
      "description": "Command template used to delete a saved waypoint alias from the active map.",
      "type": "string",
      "default": "spot.waypoint.delete"
    },
    "waypointGotoCommandName": {
      "title": "Waypoint Goto Command Name",
      "description": "Command template used for waypoint-first navigation.",
      "type": "string",
      "default": "spot.waypoint.goto"
    },
    "gotoPoseCommandName": {
      "title": "Point Goto Command Name",
      "description": "Command template used for arbitrary point navigation.",
      "type": "string",
      "default": "spot.graphnav.goto_pose"
    },
    "cancelNavCommandName": {
      "title": "Hold Position Command Name",
      "description": "Command template used to override active navigation and hold the current localized pose.",
      "type": "string",
      "default": "spot.graphnav.cancel"
    },
    "returnAndDockCommandName": {
      "title": "Return And Dock Command Name",
      "description": "Command template used to return to dock.",
      "type": "string",
      "default": "spot.return_and_dock"
    },
    "undockCommandName": {
      "title": "Undock Command Name",
      "description": "Command template used to undock the robot.",
      "type": "string",
      "default": "spot.undock"
    },
    "showWaypointLabels": {
      "title": "Show Waypoint Labels",
      "description": "Render waypoint labels on the map.",
      "type": "boolean",
      "default": true
    },
    "defaultYawMode": {
      "title": "Default Point Heading",
      "description": "Initial heading for new point selections before the operator changes it.",
      "type": "string",
      "default": "current",
      "enum": ["current", "fixed"]
    },
    "defaultYawDeg": {
      "title": "Fixed Default Heading (deg)",
      "description": "Used when Default Point Heading is fixed.",
      "type": "number",
      "default": 0
    }
  }
}
